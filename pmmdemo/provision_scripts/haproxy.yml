#cloud-config
preserve_hostname: false
hostname: ${name}
fqdn: ${fqdn}
manage_etc_hosts: true
repo_upgrade: all

packages:
  - docker

runcmd:
  - systemctl enable --now docker
  - usermod -a -G docker ec2-user
  - docker run --net host --restart=unless-stopped -d --name haproxy -v /etc/haproxy:/usr/local/etc/haproxy:ro haproxy:2.6
  - yum install -y https://repo.percona.com/yum/percona-release-latest.noarch.rpm
  - percona-release setup -y pmm2-client
  - yum install -y pmm2-client
  - pmm-admin config --metrics-mode=push --force --server-insecure-tls --server-url='https://admin:${pmm_admin_password}@${pmm_server_endpoint}' ${fqdn} generic ${name}
  - pmm-admin add haproxy --listen-port=8404 --metrics-path=/metrics

write_files:
  - path: /etc/haproxy/haproxy.cfg
    content: |
      global
        daemon
        log 127.0.0.1   local0
        log 127.0.0.1   local1 notice
        maxconn 4096
        stats socket /var/lib/haproxy/stats

      defaults
        log     global
        maxconn 2000
        mode    http
        option  dontlognull
        option  tcplog
        retries 3
        timeout client      50000
        timeout connect      5000
        timeout server      50000

      resolvers mynameservers
        parse-resolv-conf

      listen mysql-cluster
        balance roundrobin
        bind 0.0.0.0:3306 accept-proxy
        mode tcp
        option mysql-check user haproxy_check
        server pxc80-0 percona-xtradb-cluster-0:3306 rise 3 fall 3 send-proxy
        server pxc80-1 percona-xtradb-cluster-1:3306 rise 3 fall 3 send-proxy
        server pxc80-2 percona-xtradb-cluster-2:3306 rise 3 fall 3 send-proxy

      frontend stats
        bind *:8404
        http-request use-service prometheus-exporter if { path /metrics }
        stats enable
        stats refresh 10s
        stats uri /stats
